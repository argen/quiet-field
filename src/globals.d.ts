// Injected by vite `define` at build time (see vite.config.ts).
// True only for a publishable build (`QF_PUBLISH=1 vite build`); false for dev,
// the default build, and tests.
declare const __QF_PUBLISH__: boolean;
